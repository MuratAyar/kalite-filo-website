"use client";
import { useEffect,useState } from "react";
type Change={id:string;type:string;label:string;updatedAt:string|null};type Request={id:string;target:string;status:string;changeCount:number;snapshotHash:string;requestedAt:string;requestedBy:string|null};
export function PublishingCenter({csrfToken,canRequest}:{csrfToken:string;canRequest:boolean}){const[changes,setChanges]=useState<Change[]>([]);const[requests,setRequests]=useState<Request[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[submitting,setSubmitting]=useState(false);async function load(){setLoading(true);try{const response=await fetch("/admin-api/publishing.php",{credentials:"same-origin",cache:"no-store"});const payload=await response.json();if(!response.ok||!Array.isArray(payload.changes)||!Array.isArray(payload.requests))throw new Error();setChanges(payload.changes);setRequests(payload.requests);setError("");}catch{setError("Yayınlama durumu yüklenemedi.");}finally{setLoading(false);}}/* eslint-disable react-hooks/exhaustive-deps */useEffect(()=>{void load();},[]);/* eslint-enable react-hooks/exhaustive-deps */async function requestStaging(){if(window.prompt("Staging talebini dondurmak için STAGING yazın.")!=="STAGING")return;setSubmitting(true);try{const response=await fetch("/admin-api/publish-staging.php",{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/json","X-CSRF-Token":csrfToken},body:JSON.stringify({confirmation:"STAGING"})});if(!response.ok)throw new Error();await load();}catch{setError("Staging yayın talebi oluşturulamadı.");}finally{setSubmitting(false);}}return <section className="mt-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-label font-semibold text-corporate-blue">Kontrollü yayın akışı</p><h2 className="mt-1 text-heading-md">Yayınlama</h2><p className="mt-2 max-w-3xl text-sm text-text-secondary">Private değişiklikler burada dondurulur. Build ve deploy daha sonra bağlanacak harici runner üzerinde mevcut release komutlarıyla çalışacaktır.</p></div>{canRequest?<button className="min-h-11 rounded-control bg-accent-orange px-5 font-bold disabled:opacity-50" disabled={submitting||changes.length===0} onClick={()=>void requestStaging()} type="button">{submitting?"Donduruluyor...":"Staging Talebi Oluştur"}</button>:null}</div>{error?<p className="mt-5 rounded-control bg-error-surface p-4 text-error">{error}</p>:null}{loading?<p className="mt-6 text-text-secondary">Yükleniyor...</p>:<><section className="mt-6 rounded-card border bg-surface-card p-5"><div className="flex items-center justify-between"><h3 className="text-lg font-bold">Yayınlanmamış Değişiklikler</h3><span className="rounded-pill bg-surface-muted px-3 py-1 text-sm font-semibold">{changes.length}</span></div>{changes.length?<ul className="mt-4 divide-y">{changes.map(change=><li className="flex items-center justify-between gap-4 py-3" key={change.id}><div><p className="font-semibold">{change.label}</p><p className="text-xs text-text-secondary">{change.type}</p></div><time className="text-sm text-text-secondary">{change.updatedAt?new Date(change.updatedAt).toLocaleString("tr-TR"):"—"}</time></li>)}</ul>:<p className="mt-4 text-sm text-text-secondary">Yayınlanmamış private değişiklik yok.</p>}</section><section className="mt-6 rounded-card border bg-surface-card p-5"><h3 className="text-lg font-bold">Staging Talep Geçmişi</h3>{requests.length?<div className="mt-4 overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="text-text-secondary"><tr><th className="py-2 pr-4">Talep</th><th className="py-2 pr-4">Durum</th><th className="py-2 pr-4">Değişiklik</th><th className="py-2 pr-4">Snapshot</th><th className="py-2">Tarih</th></tr></thead><tbody>{requests.map(request=><tr className="border-t" key={request.id}><td className="py-3 pr-4 font-semibold">{request.id}</td><td className="py-3 pr-4">{request.status}</td><td className="py-3 pr-4">{request.changeCount}</td><td className="py-3 pr-4 font-mono text-xs">{request.snapshotHash.slice(0,12)}…</td><td className="py-3">{new Date(request.requestedAt).toLocaleString("tr-TR")}</td></tr>)}</tbody></table></div>:<p className="mt-4 text-sm text-text-secondary">Henüz staging yayın talebi yok.</p>}</section></>}</section>}
