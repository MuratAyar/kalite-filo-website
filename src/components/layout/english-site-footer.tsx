import { SiteFooter } from "./site-footer";

/** English copy of the shared public footer; layout and behaviour stay identical. */
export function EnglishSiteFooter() {
  return <SiteFooter locale="en" />;
}
