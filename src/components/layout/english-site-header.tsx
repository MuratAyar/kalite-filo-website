import { SiteHeader } from "./site-header";

/** English copy of the shared public header; layout and behaviour stay identical. */
export function EnglishSiteHeader() {
  return <SiteHeader locale="en" />;
}
